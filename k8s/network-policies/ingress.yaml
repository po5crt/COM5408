apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: edge-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: edge-server-svc
            port: 
              number: 80